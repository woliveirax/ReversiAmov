<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pt.isec.a21260792.amov_pl.reversi_isrever">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ReversiMain">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".game.GameActivity"></activity>
        <activity android:name=".Credits"></activity>
        <activity android:name=".ProfileManager"></activity>
        <activity android:name=".HistoryActivity"></activity>
        <service
            android:name=".Networking.ClientHandler"
            android:exported="false" />

    </application>

</manifest>